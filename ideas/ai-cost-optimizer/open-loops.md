# Open loops

_Questions waiting to be explored — invitations, not tasks._

- What does a week of real token spend actually look like, broken down by cause?
- Could a "context diff" be computed cheaply enough to run on every request?
